machine:
  ruby:
    version: 2.3.3
  node:
    version: v6.9.2

  services:
    - docker

test:
  pre:
    - docker build -t buildwatch --rm=false .
  post:
    - npm run coverage
    - mkdir $CIRCLE_TEST_REPORTS/junit && cp test-results.xml $CIRCLE_TEST_REPORTS/junit/test-results.xml